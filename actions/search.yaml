---
  name: search
  runner_type: "python-script"
  description: "Search term using API search endpoint"
  entry_point: "search.py"
  parameters:
    server_id:
      type: string
      description: "Server name to query."
      required: true
      default: localhost
    search_term:
      type: string
      description: search_term
      required: True
    max_result:
      type: number
      description: max_result
      required: False
