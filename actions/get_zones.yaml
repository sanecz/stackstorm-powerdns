---
  name: get_zones
  runner_type: "python-script"
  description: "List of DNS zones on a PowerDNS server"
  entry_point: "zones.py"
  parameters:
    server_id:
      type: string
      description: "Server name to query."
      required: true
      default: localhost
