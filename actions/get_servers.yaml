---
  name: get_servers
  runner_type: "python-script"
  description: "List PowerDNS servers"
  entry_point: "servers.py"
  parameters:
    server_id:
      type: string
      description: "Server name to query."
      required: true
      default: localhost
