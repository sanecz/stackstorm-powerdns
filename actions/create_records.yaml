---
  name: create_records
  runner_type: "python-script"
  description: "Create resource record sets"
  entry_point: "create_records.py"
  parameters:
    server_id:
      type: string
      description: "Server name to query."
      required: true
      default: localhost
    zone_name:
      type: string
      description: "Zone's canonical name to get details."
      required: true
    zone_name:
      type: string
      description: Zone's canonical name to get details.
      required: True
