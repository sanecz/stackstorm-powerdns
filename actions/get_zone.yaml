---
  name: get_zone
  runner_type: "python-script"
  description: "Get zone by name"
  entry_point: "get_zone.py"
  parameters:
    server_id:
      type: string
      description: "Server name to query."
      required: true
      default: localhost
    name:
      type: string
      description: Zone name (canonical)
      required: True
